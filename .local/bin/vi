#!/bin/bash

# Run vim or nvim; prefer nvim if installed

BIN=vim
if type nvim &>/dev/null; then
  BIN=nvim
fi

CMD=$(basename "$0")
if [[ $CMD == 'vi' ]] || [[ $CMD == 'vim' ]]; then
  exec $BIN "$@"
fi

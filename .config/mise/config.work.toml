# Loaded when MISE_ENV=work is exported in your shell
[tools]
terraform = "latest"
helm = "latest"
python = "3.11"
"pipx:poetry" = "latest"
node = { version = "22", postinstall = "corepack enable" }

[tasks.update_for_env]
description = "Update tools for environment: work"
run = ["claude update"]

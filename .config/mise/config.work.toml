# Loaded when MISE_ENV=work is set
[tools]
terraform = "latest"
helm = "latest"
python = "3.11"
"pipx:poetry" = "latest"
node = { version = "22" }

[tasks."update:for_env"]
description = "Update tools for environment: work"
run = ["claude update"]

[vars]
zsh_plugin_list_file = "{{ config_root }}/.config/zsh/.zsh_plugins.txt"
zsh_plugin_static_file = "{{ xdg_cache_home }}/zsh/antidote/.zsh_plugins.zsh"
wezterm_shell_integration_file = "{{ xdg_data_home }}/wezterm/wezterm.sh"

[tasks."update"]
description = "Update host-owned packages, mise tools, and managed content"
run = [
  { task = "update:host" },
  { task = "update:mise:self" },
  { task = "update:packages" },
  { task = "update:mise:tools" },
  { task = "update:managed-content" },
]

[tasks."update:mise:self"]
description = "Update mise itself"
run = "mise self-update --yes"

[tasks."update:host"]
description = "Update packages through the host operating system manager"
run = ":"

[tasks."update:packages"]
description = "Update configured direct-manager packages without crossing host ownership"
run = [{ task = "update:packages:mise" }, { task = "homebrew:install" }]

[tasks."update:packages:mise"]
description = "Update configured mise bootstrap packages on Brew-enabled hosts"
run = '''
{% if vars.dotfiles_homebrew_enabled == "true" %}
mise bootstrap packages upgrade --yes
{% else %}
:
{% endif %}
'''

[tasks."update:mise:tools"]
description = "Update mise-managed tools"
run = [
  "mise upgrade",
]

[tasks."update:managed-content"]
description = "Update managed editor, shell, repository, and CLI content"
run = [
  { tasks = [
    "update:antidote",
    "update:submodules",
    "update:nvim",
    "update:gh",
    "update:wezterm_shell_integration",
  ] },
  { task = "update:for_env" },
]

[tasks."update:antidote"]
description = "Update antidote and bundles"
run = """
#!/usr/bin/env zsh
antidote_source_dir="${ZDOTDIR:-${XDG_CONFIG_HOME:-$HOME/.config}/zsh}/.antidote"
source "$antidote_source_dir/antidote.zsh"
mkdir -p "$(dirname "{{ vars.zsh_plugin_static_file }}")"
antidote update
antidote bundle <"{{ vars.zsh_plugin_list_file }}" >|"{{ vars.zsh_plugin_static_file }}"
"""

[tasks."update:copilot"]
description = "Update GitHub Copilot CLI"
run = ["copilot update"]

[tasks."update:wezterm_shell_integration"]
description = "Refresh the cached WezTerm shell integration script when upstream changes"
run = """
dest="{{ vars.wezterm_shell_integration_file }}"
mkdir -p "$(dirname "$dest")"

tmp="${dest}.tmp"
url="https://raw.githubusercontent.com/wezterm/wezterm/main/assets/shell-integration/wezterm.sh"

if [ -f "$dest" ]; then
  if curl -fsSL -z "$dest" -o "$tmp" "$url"; then
    if [ -s "$tmp" ]; then
      mv "$tmp" "$dest"
      echo "Updated WezTerm shell integration."
    else
      rm -f "$tmp"
      echo "WezTerm shell integration already current."
    fi
  else
    rm -f "$tmp"
    echo "Failed to refresh WezTerm shell integration." >&2
    exit 1
  fi
else
  if curl -fsSL -o "$tmp" "$url"; then
    if [ -s "$tmp" ]; then
      mv "$tmp" "$dest"
      echo "Downloaded WezTerm shell integration."
    else
      rm -f "$tmp"
      echo "Downloaded empty WezTerm shell integration." >&2
      exit 1
    fi
  else
    rm -f "$tmp"
    echo "Failed to download WezTerm shell integration." >&2
    exit 1
  fi
fi
"""

[tasks."update:bat_cache"]
description = "update bat cache"
sources = ["{{ xdg_config_home }}/bat/**"]
outputs = { auto = true }
run = "bat cache --build"

[tasks."update:submodules"]
description = "Update git submodules in dotfiles repo"
run = ["git -C $DOTFILES_ROOT submodule update --remote"]

# [tasks."update:package_managers"]
# description = "Update package managers (pip, npm, etc.)"
# run = []

[tasks."nvim:python:ensure"]
description = "Ensure the Neovim Python virtual environment exists"
run = '''
if [ ! -d "${nvim_virtualenv}" ]; then
  uv venv --python python "${nvim_virtualenv}"
fi
uv pip install --upgrade -p "${nvim_virtualenv}/bin/python" pynvim
'''

[tasks."nvim:python:rebuild"]
description = "Rebuild the Neovim Python virtual environment against active mise python"
run = '''
uv venv --clear --python python "${nvim_virtualenv}"
uv pip install -p "${nvim_virtualenv}/bin/python" pynvim
'''

[tasks."update:nvim"]
description = "Update Neovim and plugins"
depends = [
  # "update:package_managers",
  "nvim:python:ensure",
]
run = [
  "nvim --headless \"+Lazy! sync\" +qa",
  "nvim --headless -c 'autocmd User MasonUpdateAllComplete quitall' -c 'MasonUpdateAll'",
]

[tasks."update:gh"]
description = "Update gh extensions if gh installed and auth'd"
run = "gh extension upgrade --all"

[tasks."update:for_env"]
description = "No-op hook for environment-specific updates"
run = ":"
